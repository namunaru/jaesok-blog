---
import "../styles/blog.css";
import { getCollection } from 'astro:content';

/* =========================
   Blog (최근 3개)
========================= */
const blogPosts = (await getCollection('blog'))
  .filter(post => !post.data.draft)
  .sort((a, b) => {
    const aTime = a.data.pubDate?.getTime() ?? 0;
    const bTime = b.data.pubDate?.getTime() ?? 0;
    return bTime - aTime;
  })
  .slice(0, 3);

/* =========================
   Video (최근 3개)
========================= */
const videoPosts = (await getCollection('video'))
  .filter(post => !post.data.draft)
  .sort((a, b) => {
    const aTime = a.data.pubDate?.getTime() ?? 0;
    const bTime = b.data.pubDate?.getTime() ?? 0;
    return bTime - aTime;
  })
  .slice(0, 3);
---

<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <title>조용한 세계문학 서재</title>

    <style>
      body {
        font-family: 'serif';
        background-color: #f9f7f2;
        color: #1a1a1a;
        margin: 0;
        line-height: 1.6;
      }

      nav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 20px 50px;
        background: white;
        border-bottom: 1px solid #eee;
      }

      .logo {
        font-weight: bold;
        font-size: 1.2rem;
      }

      .menu a {
        margin: 0 15px;
        text-decoration: none;
        color: #333;
        font-weight: 500;
      }

      .icons a {
        margin-left: 15px;
        text-decoration: none;
        font-size: 1.2rem;
        font-weight: bold;
      }

      main {
        max-width: 800px;
        margin: 80px auto;
        text-align: center;
      }

      h1 {
        font-size: 3rem;
        margin-bottom: 40px;
        font-weight: 500;
      }

      .intro-text {
        color: #444;
        margin-bottom: 60px;
        white-space: pre-line;
      }

      section {
        margin-top: 100px;
      }

      h2 {
        font-size: 1.5rem;
        margin-bottom: 30px;
      }

      ul {
        padding: 0;
        list-style: none;
        display: inline-block;
        text-align: left;
      }

      li {
        margin-bottom: 18px;
      }

      a.item-link {
        font-size: 1.15rem;
        color: #000;
        text-decoration: none;
        border-bottom: 1px solid #ccc;
        transition: 0.3s;
      }

      a.item-link:hover {
        border-bottom: 1px solid #000;
      }

      .date {
        color: #999;
        font-size: 0.8rem;
        margin-left: 12px;
      }

      footer {
        margin-top: 150px;
        padding: 50px;
        font-size: 0.9rem;
        color: #888;
        border-top: 1px solid #eee;
        text-align: center;
      }
    </style>
  </head>

  <body>
    <nav>
      <div class="logo">조용한 세계문학 서재</div>

      <div class="menu">
        <a href="/">홈</a>
        <a href="/review">북리뷰</a>
        <a href="/video">북영상</a>
      </div>

      <div class="icons">
        <a href="https://blog.naver.com/gedsc" target="_blank" style="color:#03C75A;">N</a>
        <a href="https://www.youtube.com/@gedsc" target="_blank" style="color:#FF0000;">▶</a>
      </div>
    </nav>

    <main>
      <h1>안녕하세요, gedsc입니다</h1>

      <p class="intro-text">
        이곳은 세계문학을 읽고,<br>
        그 내용을 요약하고 해석하며<br>
        개인적인 감상과 사유를 기록하는 공간입니다.
      </p>

      <!-- 블로그 -->
      <section>
        <h2>최근 기록 · 글</h2>
        {blogPosts.length === 0 ? (
          <p style="color:#999;">아직 등록된 글이 없습니다.</p>
        ) : (
          <ul>
            {blogPosts.map(post => (
              <li>
                <a class="item-link" href={`/blog/${post.slug}`}>
                  {post.data.title}
                </a>
                {post.data.pubDate && (
                  <span class="date">
                    {post.data.pubDate.toLocaleDateString()}
                  </span>
                )}
              </li>
            ))}
          </ul>
        )}
      </section>

      <!-- 영상 -->
      <section>
        <h2>최근 기록 · 영상</h2>
        {videoPosts.length === 0 ? (
          <p style="color:#999;">아직 등록된 영상이 없습니다.</p>
        ) : (
          <ul>
            {videoPosts.map(video => (
              <li>
                <a class="item-link" href={`/video/${video.slug}`}>
                  ▶ {video.data.title}
                </a>
                {video.data.pubDate && (
                  <span class="date">
                    {video.data.pubDate.toLocaleDateString()}
                  </span>
                )}
              </li>
            ))}
          </ul>
        )}
      </section>
    </main>

    <footer>
      <p>© 2025 gedsc · 세계문학 기록실</p>
      <p>일부 콘텐츠는 AI의 도움을 받아 작성되었습니다.</p>
    </footer>
  </body>
</html>
